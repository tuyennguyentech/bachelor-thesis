.claude

golang/buf/gen
golang/sql/gen

node_modules
typescript/buf/gen

# Local config overrides (may contain secrets)
golang/richter/richter.local.toml
golang/richter/richter.test.toml

# Compiled binaries accidentally placed in wrong dirs
typescript/heino/richter

# Large seed asset files (video, etc.) — download manually via scripts/seed-assets.sh
seed-assets/

# Generated test fixture video (regenerate with: espeak-ng + ffmpeg, see testdata/README)
golang/richter/testdata/*.mp4

# Compiled Go test binaries
*.test

# Implementation plans for autonomous agents (scratch input, not project artifacts)
docs/plans/
.deepseek/
