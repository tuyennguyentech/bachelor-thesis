.claude

golang/buf/gen
golang/sql/gen

node_modules
typescript/buf/gen

# Local config overrides (may contain secrets)
golang/richter/richter.local.toml
golang/richter/richter.test.toml

# Compiled binaries accidentally placed in wrong dirs
typescript/heino/richter
/richter

# Large seed asset files (video, etc.) — download manually via scripts/seed/download-assets.sh
seed-assets/

# Portable container data (postgres, fdb, seaweedfs, caddy, hf-cache) bind-mounted
# by compose.yml. NOT committed — large; transfer the folder separately so the
# project runs with pre-seeded data without re-running the pipeline.
volumes/

# Python bytecode cache (seed scripts)
__pycache__/

# Generated test fixture video (regenerate with: espeak-ng + ffmpeg, see testdata/README)
golang/richter/testdata/*.mp4

# Compiled Go test binaries
*.test

# Implementation plans for autonomous agents (scratch input, not project artifacts)
docs/plans/
.deepseek/
.gemini
.antigravitycli
.kiro
.codex

code.zip
volumes.tar.gz
