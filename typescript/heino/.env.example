# Backend URL (server-side — Node, HTTP/2)
RICHTER_BASE_URL=http://richter:8080

# Backend URL (browser-side — must start with / for origin-relative routing through Caddy)
NEXT_PUBLIC_RICHTER_BASE_URL=/api/richter

# JWT secret — must match richter's jwt.secret config
JWT_SECRET=your-jwt-secret-here
