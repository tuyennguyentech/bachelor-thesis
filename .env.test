# .env.test — TEST database overlay for Goose (DB: dyadia_test).
# Layered OVER .env.goose by scripts/setup/environment.dev/goose.sh.
# Only the connection string belongs here; shared goose config is in .env.goose.
#
# NOTE: this is the ROOT .env.test (goose only). The Playwright E2E env file is a
# DIFFERENT file at typescript/heino/.env.test (BASE_URL=http://caddy).
GOOSE_DBSTRING=postgres://dyadia_test:dyadia_test@postgres:5432/dyadia_test
